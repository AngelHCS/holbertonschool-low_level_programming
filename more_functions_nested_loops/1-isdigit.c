#include "main.h"
/**
 *_isdigit - checks for a digit
 *@c: check if it is a digit
 *
 *Return: has to be 1 if parameter is a digit 0 if not
 */

int _isdigit(int c)

{
	if (c >= '0' && c <= '9')
	{
	return (1);
	}
	else
{
	return (0);
}

}

