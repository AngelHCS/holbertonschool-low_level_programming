#include <stdio.h>
#include "pitchchar.h"
/**
* main - it has void, insert desc
*
* Return: is 0 (success)
*
*/

int main(void)
{
char ch;

for (ch = 'a' ; ch <= 'z' ; ch++)
putchar (ch);
putchar ('\n');

return (0);
}

